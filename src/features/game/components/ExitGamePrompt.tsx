// Disabling the Prompt for now because React-Router v6 doesn't support it.
// Ref: https://reactrouter.com/docs/en/v6/upgrading/v5#prompt-is-not-currently-supported

// import { Prompt } from "react-router-dom";
//
// export default function ExitGamePrompt() {
//   return (
//     <Prompt
//       message={(location) => {
//         return (
//           location.pathname.startsWith("/endgame") ||
//           "Are you sure you want to exit the game?"
//         );
//       }}
//     />
//   );
// }

export {};
